# Core image processing and generation
opencv-python
pillow
numpy
scipy
noise

# Data processing
pandas
requests

# Optional for better text processing
matplotlib

# Note: You may need to install these packages if not already available:
# pip install opencv-python pillow numpy scipy noise pandas requests matplotlib
